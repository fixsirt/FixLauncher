# ⚙️ Аргументы запуска Minecraft

## Реализованная функция

В лаунчер добавлена возможность указания **пользовательских JVM аргументов** для запуска Minecraft.

---

## 📍 Где найти

**Настройки → Аргументы запуска Minecraft**

Поле ввода находится после настройки RAM.

---

## ✅ Как использовать

### 1. Откройте настройки
Перейдите на вкладку **Настройки** (третья вкладка сверху).

### 2. Найдите поле
Прокрутите до поля **"Аргументы запуска Minecraft"**.

### 3. Введите аргументы
Примеры:
```
-XX:+UseG1GC -Djava.awt.headless=true
```

```
-XX:MaxGCPauseMillis=50 -XX:+UseStringDeduplication
```

```
-Dfml.readTimeout=180 -Dmixin.debug.export=true
```

### 4. Сохраните
Нажмите кнопку **"Сохранить"** внизу страницы настроек.

---

## 🔧 Примеры использования

### Оптимизация GC
```
-XX:+UseG1GC -XX:MaxGCPauseMillis=50
```

### Отладка модов
```
-Dmixin.debug.export=true -Dmixin.debug.countInjections=true
```

### Увеличение таймаутов
```
-Dfml.readTimeout=180 -Dfml.loginTimeout=180
```

### Производительность
```
-XX:+UseStringDeduplication -XX:+ShrinkHeapInSteps
```

### Специфичные настройки
```
-Djava.awt.headless=true -Dsun.java2d.opengl=true
```

---

## ⚠️ Важные замечания

### Не дублируйте аргументы RAM
Лаунчер автоматически добавляет:
- `-Xmx{ram}G` (максимальная память)
- `-Xms{ram}G` (начальная память)

**Не нужно** указывать `-Xmx` и `-Xms` вручную — они будут проигнорированы.

### Безопасные аргументы
Большинство JVM аргументов безопасны, но некоторые могут:
- Сломать запуск игры
- Конфликтовать с модами
- Ухудшить производительность

**Тестируйте постепенно** — добавляйте по 1-2 аргумента за раз.

### Сброс настроек
Чтобы сбросить аргументы:
1. Очистите поле ввода
2. Нажмите "Сохранить"

---

## 💾 Где хранятся

Аргументы сохраняются в **localStorage** лаунчера:
```
Key: minecraft-args
Value: "-XX:+UseG1GC -Djava.awt.headless=true"
```

**Путь к файлу localStorage:**
```
%APPDATA%\vanilla-suns\Preferences
```

---

## 🎯 Как это работает

### 1. Сохранение
```javascript
localStorage.setItem('minecraft-args', minecraftArgs || '');
```

### 2. Загрузка
```javascript
const customArgs = localStorage.getItem('minecraft-args') || '';
```

### 3. Применение при запуске
```javascript
// Разбиваем аргументы на массив
const argsArray = customArgs.match(/(?:[^\s"]+|"[^"]*")+/g) || [];

// Вставляем перед mainClass
argsArray.forEach(arg => {
    const cleanArg = arg.replace(/^"|"$/g, '');
    if (cleanArg && !cleanArg.startsWith('-Xmx') && !cleanArg.startsWith('-Xms')) {
        jvmArgs.splice(insertIndex, 0, cleanArg);
    }
});
```

### 4. Логирование
В консоли лаунчера (F12) вы увидите:
```
JVM arguments (before custom): -Xmx4G -Xms2G ...
Using custom JVM args: -XX:+UseG1GC
JVM arguments (after custom): -Xmx4G -Xms2G -XX:+UseG1GC ...
```

---

## 📋 Поддерживаемые форматы

### Простые аргументы
```
-XX:+UseG1GC
```

### Аргументы со значениями
```
-XX:MaxGCPauseMillis=50
```

### Аргументы с пробелами (в кавычках)
```
-Duser.language="en US"
```

### Несколько аргументов
```
-XX:+UseG1GC -XX:MaxGCPauseMillis=50 -Djava.awt.headless=true
```

---

## 🐛 Решение проблем

### Игра не запускается
1. Очистите поле аргументов
2. Сохраните настройки
3. Попробуйте запустить снова

### Аргументы не применяются
1. Проверьте консоль (F12)
2. Убедитесь что аргументы сохранены в localStorage
3. Проверьте синтаксис (пробелы между аргументами)

### Конфликт с модами
1. Удалите подозрительные аргументы
2. Проверьте работу без них
3. Добавляйте по одному для выявления конфликта

---

## 📚 Полезные ресурсы

- [Официальная документация Java](https://docs.oracle.com/en/java/javase/17/docs/specs/man/java.html)
- [G1GC Tuning Guide](https://wiki.openjdk.org/display/G1GC)
- [Mixin Debug Options](https://github.com/SpongePowered/Mixin/wiki/End-User-FAQ)

---

**Теперь вы можете тонко настроить Minecraft под свои нужды!** ⚙️🚀
